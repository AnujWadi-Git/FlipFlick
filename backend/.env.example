# Optional. FlipFlick works fully without this — the frontend falls back to
# a generated gradient poster when no TMDB key is set. If you want real
# poster artwork, get a free key at https://www.themoviedb.org/settings/api
# and set it here.
TMDB_API_KEY=
